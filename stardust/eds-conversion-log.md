# EDS Conversion Log — "Simply Enough" (frugality essay)

Source: `prototype/index.html` — a single-page editorial essay, "The Quiet Power of
Enough", rendered from a claude-design document-content bundle (per-element inline
styles, `data-dc-tpl` markers). Single page → block name = descriptive section role
(no cross-page reuse decisions).

## Foundation
- `styles/styles.css` rebranded: palette (ink `#1A1A1A`, cream `#F5F0EB`, terracotta
  `#C4703F`, forest `#2D4A3E`, sand `#E8DFD4`, body gray `#555`), serif =
  Cormorant Garamond, sans = DM Sans. Structural boilerplate layer kept
  (`body`/`body.appear` gate, chrome visibility reservation) but adapted for OVERLAY
  chrome (`--nav-height: 0`, `header { position: fixed }`) — the prototype header is a
  fixed translucent bar floating over the dark hero (#108), so nothing is reserved in
  flow and late chrome load shifts nothing.
- Sections are full-bleed bands: `main > .section` / `> div` zeroed so each block owns
  its own padding + inner max-width. `main .section:empty { display:none }` collapses
  the empty page-metadata section.
- Fonts self-hosted in `fonts/` (Cormorant Garamond 400/500/600 + 400-italic, DM Sans
  variable). `@font-face` in `styles/fonts.css`; metric-matched `-fallback` faces in
  `styles.css` (Cormorant→Times New Roman, DM Sans→Arial; overrides computed with
  fontTools). Both OFL — no licensing alert needed. `head.html` untouched.

## Blocks (one per distinct prototype pattern)
| Block | Section | Decode tier | Notes |
|---|---|---|---|
| `hero` (overwritten) | dark cinematic opener | template-slotted | eyebrow / h1 (single `<h1>`, `<em>` accent) / lede; generated rings + divider |
| `image-band` | 2 full-bleed image bands | reconstructive | variant `caption` (2nd instance: mountain lake w/ overlaid line) — SAME pattern collapsed to one block + variant |
| `quote` | forest-green pull quote | template-slotted | Block Collection `quote` shape; generated quote mark |
| `benefits` | "heals in three ways" | reconstructive | 3 numbered cards, one row per card; ordinal generated by index; section head = default content styled in `.benefits-container` |
| `split` | "not deprivation" | reconstructive | 2-col text+image on sand; eyebrow wrapped; generated offset frame |
| `anchors` | "five anchors" | reconstructive | 5 numbered rows; roman ordinal generated; section head = default content |
| `closing` | dark closing statement | template-slotted | generated dot-in-circle motif + divider |

Default content (D1): the intro prose band ("age of relentless accumulation…") is
authored as default content in `.section.intro`, styled in place (lead paragraph =
large serif via `:first-of-type`). Section-head eyebrow+heading above `benefits` and
`anchors` are default content, styled in place in the container.

## Chrome
- `blocks/header` rewritten: fixed translucent brand bar. Nav fragment sections:
  1 = brand link (plain `<a>`), 2 = tagline. Leaf mark SVG inlined in `header.js`
  (decorative). No nav links → no hamburger.
- `blocks/footer` reused (stock JS); CSS restyled to the "Less, but better." band.
- Fragments authored at `fragments/header.html` + `fragments/footer.html`; metadata
  references `/379de9ee06f7/nav` + `/379de9ee06f7/footer` (mapped by the deploy service).

## Images
- 3 editorial JPGs copied from `prototype/_resources/_blobs/` to `images/` (hash
  filenames) and referenced as `/images/<hash>.jpg` per task instruction. (David's-Model
  lint flags these as 🔴 D4 "not fully qualified" — expected; DA `/media` upload +
  URL rewrite is handled upstream by the calling service, not this step.)

## Justifications for 🟡 D1 advisories
`hero`, `image-band`, `closing` are single-row prose blocks BUT are genuine bespoke
widgets (full-bleed dark/photo bands, generated decorative geometry, overlaid captions)
that default-content semantics cannot express. `benefits` is a 3-row card grid — a
legitimate repeating-unit block.

## Not done (handled separately, per task)
No DA deploy, no commit, no push.
